const SuggestedPrompts = () => (<div className='flex flex-wrap gap-2'>{['What are the top 3 risks in my portfolio?','Draft a follow-up email','Which clients need outreach?','Summarize Acme meeting','Generate Q2 report'].map((prompt, i) => <button key={i} className='bg-[#0F1923] px-4 py-2 rounded text-sm hover:bg-[#16212F]'>{prompt}</button>)}</div>); export default SuggestedPrompts;